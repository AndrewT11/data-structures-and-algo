// O(n). Linear. Number of operations is proportional to n
// remember: n will be x-axis, number of operations is y-axis

function logItems(n) {
    for(let i = 0; i < n; i++) {
        console.log(i)
    }
}

logItems(10)